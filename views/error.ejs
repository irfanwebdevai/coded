<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323:wght@400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <!-- Header -->
    <header class="header">
        <nav class="navbar">
            <div class="nav-container">
                <div class="nav-brand">
                    <span class="brand-icon">🛡️</span>
                    <a href="/" style="text-decoration: none; color: inherit;">Codedx</a>
                </div>
                
                <div class="nav-actions">
                    <a href="/" class="btn-secondary">
                        <span>🏠</span>
                        <span>Home</span>
                    </a>
                </div>
            </div>
        </nav>
    </header>

    <!-- Error Section -->
    <section class="error-section">
        <div class="container">
            <div class="error-content">
                <div class="error-animation">
                    <div class="error-icon">🔍</div>
                    <div class="error-glitch">ERROR</div>
                </div>
                
                <div class="error-info">
                    <h1 class="error-title">Page Not Found</h1>
                    <p class="error-message">
                        <%= message || 'The page you are looking for does not exist. It might have been moved, deleted, or you entered the wrong URL.' %>
                    </p>
                    
                    <div class="error-code">
                        <span>ERROR CODE: 404</span>
                    </div>
                </div>
                
                <div class="error-actions">
                    <a href="/" class="btn-primary btn-large">
                        <span>🏠</span>
                        <span>Back to Home</span>
                    </a>
                    <a href="/courses/python" class="btn-secondary btn-large">
                        <span>🐍</span>
                        <span>Python Course</span>
                    </a>
                    <a href="/auth/dashboard" class="btn-secondary btn-large">
                        <span>📊</span>
                        <span>Dashboard</span>
                    </a>
                </div>
                
                <div class="error-help">
                    <h3>Need Help?</h3>
                    <ul>
                        <li>Check the URL for typos</li>
                        <li>Use the navigation menu above</li>
                        <li>Go back to the homepage and start again</li>
                        <li>Contact support if the problem persists</li>
                    </ul>
                </div>
            </div>
        </div>
    </section>

    <style>
        .error-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 120px 20px 40px;
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
        }
        
        .error-content {
            text-align: center;
            max-width: 600px;
            width: 100%;
        }
        
        .error-animation {
            margin-bottom: 3rem;
        }
        
        .error-icon {
            font-size: 6rem;
            margin-bottom: 1rem;
            animation: bounce 2s infinite;
        }
        
        .error-glitch {
            font-family: 'Press Start 2P', monospace;
            font-size: 2rem;
            color: #ef4444;
            text-shadow: 
                2px 0 #ff0000,
                -2px 0 #00ff00,
                0 2px #0000ff;
            animation: glitch 1s infinite;
        }
        
        .error-info {
            margin-bottom: 3rem;
        }
        
        .error-title {
            font-family: 'Press Start 2P', monospace;
            font-size: 2.5rem;
            color: #8b5cf6;
            margin-bottom: 1.5rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        
        .error-message {
            font-family: 'VT323', monospace;
            font-size: 1.3rem;
            color: #cbd5e1;
            line-height: 1.6;
            margin-bottom: 2rem;
        }
        
        .error-code {
            display: inline-block;
            background: rgba(239, 68, 68, 0.1);
            border: 2px solid #ef4444;
            padding: 10px 20px;
            border-radius: 8px;
            font-family: 'Press Start 2P', monospace;
            font-size: 0.8rem;
            color: #fca5a5;
            text-transform: uppercase;
        }
        
        .error-actions {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-bottom: 3rem;
        }
        
        .error-help {
            background: rgba(139, 92, 246, 0.1);
            border: 2px solid #8b5cf6;
            border-radius: 12px;
            padding: 2rem;
            text-align: left;
        }
        
        .error-help h3 {
            font-family: 'Press Start 2P', monospace;
            font-size: 1rem;
            color: #8b5cf6;
            margin-bottom: 1rem;
            text-transform: uppercase;
        }
        
        .error-help ul {
            list-style: none;
            padding: 0;
        }
        
        .error-help li {
            font-family: 'VT323', monospace;
            font-size: 1.1rem;
            color: #cbd5e1;
            margin-bottom: 0.5rem;
            padding-left: 1.5rem;
            position: relative;
        }
        
        .error-help li::before {
            content: '▶';
            position: absolute;
            left: 0;
            color: #8b5cf6;
        }
        
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-20px);
            }
            60% {
                transform: translateY(-10px);
            }
        }
        
        @keyframes glitch {
            0% {
                text-shadow: 
                    2px 0 #ff0000,
                    -2px 0 #00ff00,
                    0 2px #0000ff;
            }
            25% {
                text-shadow: 
                    -2px 0 #ff0000,
                    2px 0 #00ff00,
                    0 -2px #0000ff;
            }
            50% {
                text-shadow: 
                    0 -2px #ff0000,
                    -2px 2px #00ff00,
                    2px 0 #0000ff;
            }
            75% {
                text-shadow: 
                    2px -2px #ff0000,
                    0 2px #00ff00,
                    -2px 0 #0000ff;
            }
            100% {
                text-shadow: 
                    2px 0 #ff0000,
                    -2px 0 #00ff00,
                    0 2px #0000ff;
            }
        }
        
        @media (max-width: 768px) {
            .error-title {
                font-size: 1.8rem;
            }
            
            .error-message {
                font-size: 1.1rem;
            }
            
            .error-actions {
                flex-direction: column;
            }
            
            .error-help {
                padding: 1.5rem;
            }
        }
    </style>

    <script src="/js/script.js"></script>
</body>
</html> 